<!-- src/main/resources/logback.xml -->
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- R2 events carry their own date/time and label (5a, D10), so both R2 appenders
         use %msg%n — a pattern here would double the timestamp.
         Two appenders on one file is forbidden by logback (one-file rule), and writing
         root lines through the %msg%n FILE appender would leave them without a date/time,
         so FILE stays attached to the R2 logger only; the root logger keeps CONSOLE. -->
    <appender name="R2_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <appender name="R2_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/turbofa.log</file>
        <!-- No <append>false</append> here: logback 1.6.3 forces append=true on
             RollingFileAppender (E4, D10). Freshness is app-side: Application.kt
             truncates logs/turbofa.log at startup, before the first SLF4J logger
             initializes. This appender always appends. -->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/turbofa.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%msg%n</pattern>
        </encoder>
    </appender>

    <!-- D10: the R2 logger writes to BOTH sinks — terminal and file. -->
    <logger name="com.eduai.turbofa.requestlog" level="INFO" additivity="false">
        <appender-ref ref="R2_CONSOLE"/>
        <appender-ref ref="R2_FILE"/>
    </logger>

    <!-- Noisy libraries at WARN -->
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="io.ktor" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
